---
/**
 * ASTROBLOCKS - FEATURE COMPONENTS SHOWCASE
 *
 * Showcase page for all feature section components.
 * Each component is shown full-width with an info bar above it.
 *
 * Usage: Browse feature components, copy the ones you want to your project
 */
import PageLayout from '~/layouts/PageLayout.astro';
import Feature1, { metadata as feature1Meta, background as feature1Background } from '~/components/sections/Feature1.astro';
import Feature2, { metadata as feature2Meta, background as feature2Background } from '~/components/sections/Feature2.astro';
import Feature3, { metadata as feature3Meta, background as feature3Background } from '~/components/sections/Feature3.astro';
import Feature4, { metadata as feature4Meta, background as feature4Background } from '~/components/sections/Feature4.astro';
import Feature5, { metadata as feature5Meta, background as feature5Background } from '~/components/sections/Feature5.astro';
import Feature6, { metadata as feature6Meta, background as feature6Background } from '~/components/sections/Feature6.astro';
import Feature7, { metadata as feature7Meta, background as feature7Background } from '~/components/sections/Feature7.astro';
import Feature8, { metadata as feature8Meta, background as feature8Background } from '~/components/sections/Feature8.astro';
import Feature9, { metadata as feature9Meta, background as feature9Background } from '~/components/sections/Feature9.astro';
import Feature10, { metadata as feature10Meta, background as feature10Background } from '~/components/sections/Feature10.astro';
---

<PageLayout
  metadata={{
    title: 'Feature Components - AstroBlocks',
    description: 'Feature section components with various layouts, icons, and images.',
  }}
>
  <!-- Page Header -->
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="text-center">
      <h1 class="text-4xl font-bold text-white mb-4">Feature Components</h1>
      <p class="text-lg text-gray-400">
        Feature sections to showcase product capabilities and benefits
      </p>
    </div>
  </div>

  <!-- Component Previews (full-width) -->
  <div class="space-y-0">

    <!-- ============================================ -->
    <!-- FEATURE1 COMPONENT                         -->
    <!-- ============================================ -->
    <div>
      <!-- INFO BAR: Black background, white text, component metadata -->
      <div class="bg-black border-y border-gray-800 py-4">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 class="text-lg font-semibold text-white mb-1">{feature1Meta.name}</h2>
          <p class="text-sm text-gray-300">{feature1Meta.description}</p>
          <code class="text-sm font-mono text-gray-400 mb-2 block">{feature1Meta.path}</code>
          <hr />
        </div>
      </div>

      <!-- LIVE COMPONENT: Full-width, ready to copy -->
      <div class={feature1Background}>
        <Feature1 />
      </div>
    </div>
    <!-- ============================================ -->
    <!-- END FEATURE1 COMPONENT                     -->
    <!-- ============================================ -->

    <!-- ============================================ -->
    <!-- FEATURE2 COMPONENT                         -->
    <!-- ============================================ -->
    <div>
      <!-- INFO BAR: Black background, white text, component metadata -->
      <div class="bg-black border-y border-gray-800 py-4">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 class="text-lg font-semibold text-white mb-1">{feature2Meta.name}</h2>
          <p class="text-sm text-gray-300">{feature2Meta.description}</p>
          <code class="text-sm font-mono text-gray-400 mb-2 block">{feature2Meta.path}</code>
          <hr />
        </div>
      </div>

      <!-- LIVE COMPONENT: Full-width, ready to copy -->
      <div class={feature2Background}>
        <Feature2 />
      </div>
    </div>
    <!-- ============================================ -->
    <!-- END FEATURE2 COMPONENT                     -->
    <!-- ============================================ -->

    <!-- ============================================ -->
    <!-- FEATURE3 COMPONENT                         -->
    <!-- ============================================ -->
    <div>
      <!-- INFO BAR: Black background, white text, component metadata -->
      <div class="bg-black border-y border-gray-800 py-4">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 class="text-lg font-semibold text-white mb-1">{feature3Meta.name}</h2>
          <p class="text-sm text-gray-300">{feature3Meta.description}</p>
          <code class="text-sm font-mono text-gray-400 mb-2 block">{feature3Meta.path}</code>
          <hr />
        </div>
      </div>

      <!-- LIVE COMPONENT: Full-width, ready to copy -->
      <div class={feature3Background}>
        <Feature3 />
      </div>
    </div>
    <!-- ============================================ -->
    <!-- END FEATURE3 COMPONENT                     -->
    <!-- ============================================ -->

    <!-- ============================================ -->
    <!-- FEATURE4 COMPONENT                         -->
    <!-- ============================================ -->
    <div>
      <!-- INFO BAR: Black background, white text, component metadata -->
      <div class="bg-black border-y border-gray-800 py-4">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 class="text-lg font-semibold text-white mb-1">{feature4Meta.name}</h2>
          <p class="text-sm text-gray-300">{feature4Meta.description}</p>
          <code class="text-sm font-mono text-gray-400 mb-2 block">{feature4Meta.path}</code>
          <hr />
        </div>
      </div>

      <!-- LIVE COMPONENT: Full-width, ready to copy -->
      <div class={feature4Background}>
        <Feature4 />
      </div>
    </div>
    <!-- ============================================ -->
    <!-- END FEATURE4 COMPONENT                     -->
    <!-- ============================================ -->

    <!-- ============================================ -->
    <!-- FEATURE5 COMPONENT                         -->
    <!-- ============================================ -->
    <div>
      <!-- INFO BAR: Black background, white text, component metadata -->
      <div class="bg-black border-y border-gray-800 py-4">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 class="text-lg font-semibold text-white mb-1">{feature5Meta.name}</h2>
          <p class="text-sm text-gray-300">{feature5Meta.description}</p>
          <code class="text-sm font-mono text-gray-400 mb-2 block">{feature5Meta.path}</code>
          <hr />
        </div>
      </div>

      <!-- LIVE COMPONENT: Full-width, ready to copy -->
      <div class={feature5Background}>
        <Feature5 />
      </div>
    </div>
    <!-- ============================================ -->
    <!-- END FEATURE5 COMPONENT                     -->
    <!-- ============================================ -->

    <!-- ============================================ -->
    <!-- FEATURE6 COMPONENT                         -->
    <!-- ============================================ -->
    <div>
      <!-- INFO BAR: Black background, white text, component metadata -->
      <div class="bg-black border-y border-gray-800 py-4">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 class="text-lg font-semibold text-white mb-1">{feature6Meta.name}</h2>
          <p class="text-sm text-gray-300">{feature6Meta.description}</p>
          <code class="text-sm font-mono text-gray-400 mb-2 block">{feature6Meta.path}</code>
          <hr />
        </div>
      </div>

      <!-- LIVE COMPONENT: Full-width, ready to copy -->
      <div class={feature6Background}>
        <Feature6 />
      </div>
    </div>
    <!-- ============================================ -->
    <!-- END FEATURE6 COMPONENT                     -->
    <!-- ============================================ -->

    <!-- ============================================ -->
    <!-- FEATURE7 COMPONENT                         -->
    <!-- ============================================ -->
    <div>
      <!-- INFO BAR: Black background, white text, component metadata -->
      <div class="bg-black border-y border-gray-800 py-4">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 class="text-lg font-semibold text-white mb-1">{feature7Meta.name}</h2>
          <p class="text-sm text-gray-300">{feature7Meta.description}</p>
          <code class="text-sm font-mono text-gray-400 mb-2 block">{feature7Meta.path}</code>
          <hr />
        </div>
      </div>

      <!-- LIVE COMPONENT: Full-width, ready to copy -->
      <div class={feature7Background}>
        <Feature7 />
      </div>
    </div>
    <!-- ============================================ -->
    <!-- END FEATURE7 COMPONENT                     -->
    <!-- ============================================ -->

    <!-- ============================================ -->
    <!-- FEATURE8 COMPONENT                         -->
    <!-- ============================================ -->
    <div>
      <!-- INFO BAR: Black background, white text, component metadata -->
      <div class="bg-black border-y border-gray-800 py-4">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 class="text-lg font-semibold text-white mb-1">{feature8Meta.name}</h2>
          <p class="text-sm text-gray-300">{feature8Meta.description}</p>
          <code class="text-sm font-mono text-gray-400 mb-2 block">{feature8Meta.path}</code>
          <hr />
        </div>
      </div>

      <!-- LIVE COMPONENT: Full-width, ready to copy -->
      <div class={feature8Background}>
        <Feature8 />
      </div>
    </div>
    <!-- ============================================ -->
    <!-- END FEATURE8 COMPONENT                     -->
    <!-- ============================================ -->

    <!-- ============================================ -->
    <!-- FEATURE9 COMPONENT                         -->
    <!-- ============================================ -->
    <div>
      <!-- INFO BAR: Black background, white text, component metadata -->
      <div class="bg-black border-y border-gray-800 py-4">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 class="text-lg font-semibold text-white mb-1">{feature9Meta.name}</h2>
          <p class="text-sm text-gray-300">{feature9Meta.description}</p>
          <code class="text-sm font-mono text-gray-400 mb-2 block">{feature9Meta.path}</code>
          <hr />
        </div>
      </div>

      <!-- LIVE COMPONENT: Full-width, ready to copy -->
      <div class={feature9Background}>
        <Feature9 />
      </div>
    </div>
    <!-- ============================================ -->
    <!-- END FEATURE9 COMPONENT                     -->
    <!-- ============================================ -->

    <!-- ============================================ -->
    <!-- FEATURE10 COMPONENT                        -->
    <!-- ============================================ -->
    <div>
      <!-- INFO BAR: Black background, white text, component metadata -->
      <div class="bg-black border-y border-gray-800 py-4">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 class="text-lg font-semibold text-white mb-1">{feature10Meta.name}</h2>
          <p class="text-sm text-gray-300">{feature10Meta.description}</p>
          <code class="text-sm font-mono text-gray-400 mb-2 block">{feature10Meta.path}</code>
          <hr />
        </div>
      </div>

      <!-- LIVE COMPONENT: Full-width, ready to copy -->
      <div class={feature10Background}>
        <Feature10 />
      </div>
    </div>
    <!-- ============================================ -->
    <!-- END FEATURE10 COMPONENT                    -->
    <!-- ============================================ -->

  </div>
</PageLayout>
