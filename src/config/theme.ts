/**
 * ============================================================================
 * THEME CONFIGURATION
 * ============================================================================
 *
 * Centralized theme settings for consistent styling across all components.
 * Modify these values to change the appearance of all components at once.
 * ============================================================================
 */

/**
 * Default section background color/gradient
 * Applied to all section components for consistent appearance
 */
export const SECTION_BACKGROUND = 'bg-gradient-to-b from-slate-900 via-slate-900 to-black';

/**
 * Typography scales - consistent responsive text sizing
 */
export const HEADLINE_CLASSES = 'text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold tracking-tight';
export const SUBTITLE_CLASSES = 'text-base sm:text-lg md:text-xl leading-relaxed';

/**
 * Text colors - consistent color palette
 */
export const TEXT_PRIMARY = 'text-white';
export const TEXT_MUTED = 'text-slate-400';

/**
 * Border colors - consistent border styling
 */
export const BORDER_DEFAULT = 'border-slate-700/50';
